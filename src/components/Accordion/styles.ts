import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        overflow: 'hidden',
    },
    contentContainer: {
        position: 'absolute',
        width: '100%',
        top: 0,
    },
});

export default styles;
